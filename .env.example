# CivicIQ Environment Variables
# Copy to .env and fill in your values

# Gemini API Key (for Claude LLM narrative generation)
# If not provided, the system falls back to deterministic mock narratives
GEMINI_API_KEY=your_gemini_api_key_here

# Server configuration
HOST=0.0.0.0
PORT=8000
